function _main() =
  (
    let
      function print_conditional(s : string, i : int) =
        if i then
          print(s)
        else
          print("error")
      var str := "salut mon pote"
    in
      print_conditional("foo", 1)
    end;
    ()
  )